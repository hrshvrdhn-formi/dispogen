"""dispogen — disposition test-case generator.

Deterministic Python decides what to test and against which rival; a routed
model writes the conversation; an independent panel verifies the label; and
everything uncertain is routed to the Ambiguous Register rather than guessed.
"""
__version__ = "0.2.0"
